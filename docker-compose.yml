version: '3'

services:
  app:
    build: ./app
    ports:
      - "5000:5000"
    depends_on:
      - metadata
      - storage

  metadata:
    build: ./metadata
    container_name: metadata

  storage:
    build: ./storage
    container_name: storage
